server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dishes_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "dishes"
appVersion: "1.0.0"

restaurant:
  service:
    url: http://localhost:8082
